# Environment variables for saas-wrapper-backend systemd service

# Port for the Go HTTP server (required)
PORT=18000

# Directory for persistent storage (uploads, thumbnails). Optional.
# If unset, defaults to ./storage relative to WorkingDirectory.
# STORAGE_DIR=/var/www/vhosts/social.portnumber53.com/storage

# Optional shared secret for DELETE /api/uploads/<name> protection.
# If set, clients must send header: X-Backend-Secret: $BACKEND_SECRET
# BACKEND_SECRET=change-me

